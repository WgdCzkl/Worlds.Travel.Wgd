@model Worlds.Travel.Web.Models.ComeTo.ComeToArchitectureViewModel

@{
    ViewBag.Title = "ComeToArchitecture";
}

<form id="form-login" class="p-t-15" action="@Url.Action("ComeToStorey", "ComeTo")" , method="POST">
    选择楼层
    <select name="keyName">
        @foreach (var item in Model.Storeys)
        {
            <option value="@item.Key">@item.Name</option>
        }
    </select>
    <button class="btn btn-primary btn-cons m-t-10" type="submit">降临</button>
</form>


